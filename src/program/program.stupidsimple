this is input;
this is sum;
this is index;
input has 5;
sum has 0;
index has 1;
loop while (index is less than input)
    sum increase with index;
    index equals index plus 1;
end while;
print sum;
